# Tecan 裁决规则

- 单据事实优先于主数据；主数据只补缺失，不覆盖本票事实。
- 只有唯一的料号/编码等非语义标识允许匹配；名称相似、多个候选或跨行猜测均不可写入正式字段。
- 同币种同商品行才可由数量、单价、总价确定性补算；多币种不汇总、不换算。
- 缺少非核心字段时保留 `null`，并在 `problems` 给出来源、位置、问题与补充动作。
- 同票多值按材料出现顺序用英文逗号连接；不要输出候选值、置信度或审计轨迹。
